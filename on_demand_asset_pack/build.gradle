// In the asset pack’s build.gradle file:
apply plugin: 'com.android.asset-pack'

assetPack {
    packName = "on_demand_asset_pack" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "on-demand"
    }
}